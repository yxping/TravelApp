<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:layout_gravity="center"
    android:orientation="vertical" >

    <LinearLayout
        android:layout_width="250dp"
        android:layout_height="150dp"
        android:background="@drawable/me_setting_nickname_frame"
        android:gravity="center"
        android:orientation="vertical" >

        <EditText
            android:id="@+id/me_setting_password_text"
            android:inputType="textPassword"
            android:layout_width="220dp"
            android:layout_height="50dp"
            android:background="@drawable/gray_edittext_bg"
            android:hint="@string/me_account_text_hint" >
        </EditText>
        <EditText
            android:id="@+id/me_setting_password_retext"
            android:inputType="textPassword"
            android:layout_width="220dp"
            android:layout_height="50dp"
            android:background="@drawable/gray_edittext_bg"
            android:hint="@string/me_account_text_hint"
            android:layout_marginTop="10dp" >
        </EditText>
    </LinearLayout>

    <Button
        android:id="@+id/me_setting_password_btn"
        android:layout_width="250dp"
        android:layout_height="40dp"
        android:layout_marginTop="20dp"
        android:background="@drawable/me_setting_btn_shape"
        android:text="确认"
        android:textColor="@color/white" />

</LinearLayout>